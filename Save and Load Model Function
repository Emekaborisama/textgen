def saveModel(model):
    '''Saves the trained model'''
    model.save('./rosaLove.h5')
    trainedModel = './rosaLove.h5'
    return trainedModel 
		
		
def loadModel(trainedModel):
    '''Loads the saved model'''
    
    model = keras.models.load_model(trainedModel)
    return model
